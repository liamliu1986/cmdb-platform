apiVersion: v1
kind: Service
metadata:
  name: cmdb-api
  namespace: {{ .Values.namespace }}
  labels:
    app: cmdb-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app: cmdb-api