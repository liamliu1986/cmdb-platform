apiVersion: v1
kind: Service
metadata:
  name: cmdb-ui
  namespace: {{ .Values.namespace }}
  labels:
    app: cmdb-ui
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app: cmdb-ui